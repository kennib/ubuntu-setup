set exrc            " enable per-directory .vimrc files
set secure          " disable unsafe commands in local .vimrc files

set tabstop=4       " Set tabs to width of 4 spaces
set shiftwidth=4    " 

set mouse=a         " enable mouse navigation
